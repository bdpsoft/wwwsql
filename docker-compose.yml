version: "3.9"

services:
  qr-scanner:
    image: node:20
    container_name: wwwsql
    working_dir: /app

    volumes:
      - ./:/app

    command: ["node", "server.js"]

  
    ports:
      - "3000:3000"